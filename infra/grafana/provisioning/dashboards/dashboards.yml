apiVersion: 1

providers:
  - name: "codex-home"
    orgId: 1
    folder: "Codex Home"
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    options:
      path: /etc/grafana/provisioning/dashboards/json
